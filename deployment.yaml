apiVersion: apps/v1
kind: Deployment
metadata:
  name: oms-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oms
  template:
    metadata:
      labels:
        app: oms
    spec:
      containers:
      - name: oms-container
        image: raashidcodes/oms-core:latest # We will push this soon
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: oms-service
spec:
  type: NodePort
  selector:
    app: oms
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30001
